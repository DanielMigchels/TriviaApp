name: Build Application

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

env:
  DOTNET_VERSION: '9.0.x'
  NODE_VERSION: '24.x'

jobs:

  Build:
    name: Build Application
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: ${{ env.NODE_VERSION }}
        cache: 'npm'
        cache-dependency-path: './TriviaApp.UI/package-lock.json'

    - name: Install Angular dependencies
      working-directory: ./TriviaApp.UI
      run: npm ci

    - name: Test Angular
      working-directory: ./TriviaApp.UI
      run: npm run test -- --watch=false --browsers=ChromeHeadless || echo "No tests configured"

    - name: Build Angular (Production)
      working-directory: ./TriviaApp.UI
      run: npm run build -- --configuration production

    - name: Copy Angular build to API wwwroot
      run: |
        mkdir -p ./TriviaApp.API/TriviaApp.API/wwwroot/
        rm -rf ./TriviaApp.API/TriviaApp.API/wwwroot/*
        cp -r ./TriviaApp.UI/dist/TriviaApp.UI/browser/* ./TriviaApp.API/TriviaApp.API/wwwroot/ || cp -r ./TriviaApp.UI/dist/* ./TriviaApp.API/TriviaApp.API/wwwroot/

    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: ${{ env.DOTNET_VERSION }}

    - name: Restore .NET dependencies
      working-directory: ./TriviaApp.API
      run: dotnet restore

    - name: Build .NET API
      working-directory: ./TriviaApp.API
      run: dotnet build --configuration Release --no-restore

    - name: Test .NET API
      working-directory: ./TriviaApp.API
      run: dotnet test --configuration Release --no-build --verbosity normal --collect:"XPlat Code Coverage"

    - name: Publish .NET API as executable (Windows)
      working-directory: ./TriviaApp.API
      run: |
        dotnet publish TriviaApp.API/TriviaApp.API.csproj \
          --configuration Release \
          --runtime win-x64 \
          --self-contained true \
          --output ./publish/win-x64 \
          -p:PublishSingleFile=true \
          -p:IncludeNativeLibrariesForSelfExtract=true

    - name: Publish .NET API as executable (Linux)
      working-directory: ./TriviaApp.API
      run: |
        dotnet publish TriviaApp.API/TriviaApp.API.csproj \
          --configuration Release \
          --runtime linux-x64 \
          --self-contained true \
          --output ./publish/linux-x64 \
          -p:PublishSingleFile=true \
          -p:IncludeNativeLibrariesForSelfExtract=true

    - name: Upload Windows artifacts
      uses: actions/upload-artifact@v4
      with:
        name: triviaapp-windows-x64
        path: ./TriviaApp.API/publish/win-x64
        retention-days: 7

    - name: Upload Linux artifacts
      uses: actions/upload-artifact@v4
      with:
        name: triviaapp-linux-x64
        path: ./TriviaApp.API/publish/linux-x64
        retention-days: 7